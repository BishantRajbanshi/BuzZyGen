/* Form card styling */
.form-card {
    background: rgba(51, 51, 51, 0.5);
    padding: 1rem;
    /* Padding to reduce card size */
    border-radius: 20px;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
    text-align: center;
    color: white;
    width: 35vw;
    /* Adjusted width */
    max-width: 450px;
    /* Slightly smaller max-width */
    min-width: 250px;
    max-height: 500px;
    /* Adjusted height */
    backdrop-filter: blur(10px);
    overflow: hidden;
}